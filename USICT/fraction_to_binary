#include <stdio.h>
int main(void)
{
    int lst[32];
    int intpart, j=0;
    double number, fracpart;
    printf("Enter a fractional number - ");
    scanf("%lf",&number);
    intpart = int(number);
    fracpart = number - intpart;

    //integer part
    while(intpart>0)
    {
        lst[j] = number%2;
        number = number/2;
        j++;
    }

    //fraction part
    while(fracpart)
    
}